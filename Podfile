# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :ios, '18.0'

target 'mySHiT' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mySHiT
  # pod 'Firebase/Core'
  # pod 'Firebase/Messaging'

  target 'mySHiTTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mySHiTUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

#target 'SHiTNotifications' do
  #inherit! :search_paths
#  use_frameworks!
  
#  pod 'Firebase/Core'
#  pod 'Firebase/Messaging'
#end
